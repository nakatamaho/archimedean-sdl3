# Specification

## Purpose

`archimedean-sdl3` demonstrates five Platonic solids and all 13 Archimedean
solids using polygon data generated by SageMath and displayed by a portable
SDL3 viewer. The v1.2 data set therefore contains 18 regular solids.

## Architectural split

### Geometry pipeline

SageMath -> canonical polygon topology -> deterministic JSON -> pure-Python validation.

### Viewer pipeline

JSON -> validated model -> CPU 3D transform -> face culling -> flat Lambert shading -> polygon fan triangulation -> depth sort -> perspective projection -> SDL3 `SDL_RenderGeometry()`.

The viewer must never depend on SageMath at runtime.

## Canonical solids and combinatorics

| ID | V | E | F | Faces |
|---|---:|---:|---:|---|
| `tetrahedron` | 4 | 6 | 4 | 4 triangles |
| `cube` | 8 | 12 | 6 | 6 squares |
| `octahedron` | 6 | 12 | 8 | 8 triangles |
| `dodecahedron` | 20 | 30 | 12 | 12 pentagons |
| `icosahedron` | 12 | 30 | 20 | 20 triangles |
| `truncated_tetrahedron` | 12 | 18 | 8 | 4 triangles, 4 hexagons |
| `cuboctahedron` | 12 | 24 | 14 | 8 triangles, 6 squares |
| `truncated_cube` | 24 | 36 | 14 | 8 triangles, 6 octagons |
| `truncated_octahedron` | 24 | 36 | 14 | 6 squares, 8 hexagons |
| `rhombicuboctahedron` | 24 | 48 | 26 | 8 triangles, 18 squares |
| `truncated_cuboctahedron` | 48 | 72 | 26 | 12 squares, 8 hexagons, 6 octagons |
| `snub_cube` | 24 | 60 | 38 | 32 triangles, 6 squares |
| `icosidodecahedron` | 30 | 60 | 32 | 20 triangles, 12 pentagons |
| `truncated_dodecahedron` | 60 | 90 | 32 | 20 triangles, 12 decagons |
| `truncated_icosahedron` | 60 | 90 | 32 | 12 pentagons, 20 hexagons |
| `rhombicosidodecahedron` | 60 | 120 | 62 | 20 triangles, 30 squares, 12 pentagons |
| `truncated_icosidodecahedron` | 120 | 180 | 62 | 30 squares, 20 hexagons, 12 decagons |
| `snub_dodecahedron` | 60 | 150 | 92 | 80 triangles, 12 pentagons |

The first five entries are the Platonic solids added in v1.2. Mirrored snub
forms are not counted as additional release solids. Optional mirror generation
may be added only after the current release acceptance.

## Geometry tolerances

Document actual chosen values beside the implementation. Initial targets:

- normalized edge spread: <= `1e-8` relative where practical, otherwise justify a looser threshold for approximate Sage constructors;
- face planarity: <= `1e-9 * characteristic_radius` where practical;
- vector normalization zero threshold: `1e-12`.

Do not silently widen a tolerance to make failing data pass. Record why a change is necessary.

## Viewer behavior

The solid continuously rotates using elapsed time. The user can edit the normalized rotation axis and angular speed while the program is running. Each polygon is opaque and flat-shaded.

For a convex solid, v1 uses back-face culling plus back-to-front visible-face sorting instead of a hardware depth buffer. If a concrete artifact demonstrates incorrect occlusion, replace this renderer in a later milestone; do not preemptively introduce a platform-specific graphics API.

## Error handling

Startup must fail clearly on:

- missing JSON;
- malformed JSON;
- unsupported schema version;
- missing requested solid;
- invalid vertex index;
- non-finite coordinate;
- topological inconsistency detected by the runtime loader.

SDL errors must include `SDL_GetError()` text where relevant.
